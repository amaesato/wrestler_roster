<h1><%= @wrestler.name %></h1>
<h3>Edit Profile</h3>
<%= render(partial: 'form') %>

<%= link_to '<< Back', wrestler_path(@wrestler) %> |
<%= link_to 'Delete Profile', wrestler_path(@wrestler), method: :delete, data: {confirm: 'Are you sure you want to delete your profile?'} %>
